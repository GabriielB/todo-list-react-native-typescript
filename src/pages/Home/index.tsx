import React from "react";
import { View } from "react-native";
import {Header} from '../../components/Header/Header'

export const Home = () => {
    return (
        <View>
            <Header/>
        </View>
    )
}